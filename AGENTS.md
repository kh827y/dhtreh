На данном этапе работы доводим мерчант портал до финальной точки разработки (до продакшена):
переводим на новый дизайн (папка new design, новый интерфейс должен быть в точности такой же как в этой папке, а не "приближённый". включая все названия, тексты, иконки, стили и всё остальное. я специально в этом дизайне уделял внимание мелочам, нужно полностью перенести этот дизайн), тщательно проверяем код каждой страницы на баги и соответствие продакшн нормам, фиксим недоработки, удаляем заглушки/код работающий только локально, удаляем ненужный legacy функционал, который не нужен для мерчант портала (интерфейс этой части проекта уже реализован окончательно, поэтому излишний код который не служит реализацией какого-либо элемента во фронтенде можно удалить: проверяем, если не используется в новом дизайне мерчант портала, то избавляемся от этого кода), пишем развернутые тесты под каждую актуальную часть мерчант портала, которая реально используется во фронтенде. Также разрабатывыем E2E тесты для мерчант портала. Разрешаю править api, разрешаю объединять код для разных частей проекта, если считаешь что так будет оптимальнее и более грамотно с точки зрения структуры кодовой базы (например вынести логику которую одинаково реализуют разные страницы портала в один хелпер и тд). Оптимизируем кодовую базу, избавляемся от костылей. Делаем человекочитаемые ошибки, чтобы пользователь мерчант портала не видел необработанных сообщений об ошибках. В интерфейсе добавляем где необходимо перенос текста чтобы не выходил за рамки блока, и делаем там где нужно пагинацию/скролл таблиц и других элементов и так далее (в общем прорабатываем интерфейс). Если придется, дорабатывай бэкенд для нового интерфейса. Новый интерфейс нужно перенести полностью, весь функционал должен работать показанный в новом интерфейсе.
Можешь использовать plan.md для записи, чтобы не потерять из контекста ничего важного. 
Отвечай на русском языке.