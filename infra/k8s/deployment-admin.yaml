apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin
  namespace: loyalty
  labels:
    app: loyalty-admin
spec:
  replicas: 2
  selector:
    matchLabels:
      app: loyalty-admin
  template:
    metadata:
      labels:
        app: loyalty-admin
    spec:
      containers:
        - name: admin
          image: ghcr.io/your-org/loyalty-admin:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          envFrom:
            - configMapRef:
                name: admin-config
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
