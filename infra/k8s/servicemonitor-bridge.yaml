apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: bridge
  namespace: loyalty
  labels:
    app: loyalty-bridge
    release: prometheus
spec:
  selector:
    matchLabels:
      app: loyalty-bridge
  namespaceSelector:
    matchNames:
      - loyalty
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
