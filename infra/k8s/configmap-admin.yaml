apiVersion: v1
kind: ConfigMap
metadata:
  name: admin-config
  namespace: loyalty
  labels:
    app: loyalty-admin
data:
  NODE_ENV: "production"
  NEXT_PUBLIC_API_BASE_URL: "https://api.example.com"
