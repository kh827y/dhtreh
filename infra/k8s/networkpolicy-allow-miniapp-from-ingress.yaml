apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-miniapp-from-ingress
  namespace: loyalty
spec:
  podSelector:
    matchLabels:
      app: loyalty-miniapp
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
          podSelector: {}
      ports:
        - protocol: TCP
          port: 3000
