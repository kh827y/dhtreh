apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: loyalty
  labels:
    app: loyalty-api
spec:
  selector:
    app: loyalty-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
