# Merchant Portal environment example
# Base URL of the API that serves /portal/* (NestJS). Optional here.
# NEXT_PUBLIC_API_BASE=http://localhost:3000

# Customers microservice base used by Next.js API route /api/customers/*
# Defaults to http://localhost:3004 when not set
CUSTOMERS_API_BASE=http://localhost:3004
