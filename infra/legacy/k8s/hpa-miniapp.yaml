apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: miniapp
  namespace: loyalty
  labels:
    app: loyalty-miniapp
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: miniapp
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
