module.exports = {
  root: true,
  extends: ['next/core-web-vitals'],
  ignorePatterns: ['.next', 'node_modules', 'dist'],
  rules: {
    // Мягкие правила, ужесточение — по мере стабилизации
  },
};
